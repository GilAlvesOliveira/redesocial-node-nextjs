/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  /* typescript: {
    ibnoreBuildErrors: true        ....aqui seria para elimiar o erro quando for sunbir apra o deploy 
   } */
}

module.exports = nextConfig
